function Home() {
  return (
    <header style={styles.container}>
      <h1 style={styles.title}>devfinder</h1>
    </header>
  );
}

const styles = {
  container: {
    marginBottom: '40px',
  },
  title: {
    fontSize: '1.5em',
  }
}

export default Home;
